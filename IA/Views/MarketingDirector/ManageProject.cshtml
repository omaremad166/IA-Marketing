@model IA.Models.Project

@{
    ViewBag.Title = "ManageProject";
}

<div class="content-wrap">
    <div class="main">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xl-8 col-lg-8 col-md-8 col-sm-12 col-12 p-r-0 title-margin-right"></div>
                <!-- /# column -->
                <div class="col-xl-4 col-lg-4 col-md-4 col-sm-12 col-12 p-l-0 title-margin-left">
                    <div class="page-header page_header_2">
                        <div class="page-title">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item"><a href="#">Dashboard</a></li>
                                <li class="breadcrumb-item active">Edit</li>
                            </ol>
                        </div>
                    </div>
                </div>
                <!-- /# column -->
            </div>
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-title">
                            <h4>Edit Project</h4>
                        </div>
                        <div class="card-body">
                            <div class="basic-elements">
                                @using (Html.BeginForm("ManageProject", "MarketingDirector"))
                                {
                                    <div class="row">
                                        <div class="col-lg-6">
                                            @Html.HiddenFor(model => model.ProjectId)
                                            @Html.HiddenFor(model => model.ProjectStateId)
                                            @Html.HiddenFor(model => model.Description)
                                            @Html.HiddenFor(model => model.ProjectName)
                                            <div class="form-group">
                                                @Html.LabelFor(m => m.Price)
                                                @Html.TextBoxFor(m => m.Price, new { @class = "form-control" })
                                            </div>
                                            <div class="form-group">
                                                @Html.LabelFor(m => m.StartDate)
                                                <input type="date" value="@Model.StartDate" name="StartDate" class="form-control"/>
                                            </div>
                                            <div class="form-group">
                                                @Html.LabelFor(m => m.EndDate)
                                                <input type="date" value="@Model.EndDate" name="EndDate" class="form-control"/>
                                            </div>
                                            <div class="form-group">
                                                @Html.ActionLink("Back", "MarketingDirectorProfile", "MarketingDirector", null, new { @class = "btn" })
                                                <button type="submit" class="btn btn-primary">Submit</button>
                                            </div>
                                        </div>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>



